const PI = 3.14;
const arr = ["dos", "cuatro", "ocho", "diez"];
function sumar(x, y){return parseInt(x) + parseInt(y)}
function restar(x, y){return parseInt(x) - parseInt(y)}
const multiplicar = (x, y) => {return parseInt(x) * parseInt(y)}
const dividir = (x, y) => {return parseInt(x) / parseInt(y)};

export {PI, arr, sumar, restar, multiplicar, dividir};